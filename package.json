{
  "name": "xy",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "docs:dev": "xy-tools docs dev",
    "docs:build": "xy-tools docs build",
    "lint": "eslint --ext .js packages",
    "build": "xy-tools build theme && xy-tools build component",
    "cli:watch": "tsc -w -p ./packages/cli/",
    "cli:build": "tsc -b ./packages/cli/",
    "utils:watch": "tsc -w -p ./packages/utils/",
    "utils:build": "tsc -b ./packages/utils/"
  },
  "dependencies": {
    "@xiyun/xy-element-ui": "^1.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "lerna": "^3.11.1",
    "less-loader": "^4.1.0",
    "lint-staged": "^8.1.5",
    "prettier": "^1.17.0",
    "vue-template-compiler": "^2.6.10",
    "xy-tools": "^0.0.7"
  }
}
